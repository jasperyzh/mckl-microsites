name: Deploy to production via SFTP
on:
  push:
    branches:
      - production
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build:
    name: build
    runs-on: ubuntu-latest
    env:
      NODE_ENV: production
    steps:
      - name: Checkout repository
        uses: actions/checkout@main
      - name: Install dependencies
        run: npm ci
      - name: Build dependencies
        run: npm run build
      - name: Archive production artifact
        uses: actions/upload-artifact@main
        with:
          name: dist
          path: dist


  deploy_sftp:
    name: deploy_sftp
    needs: build
    runs-on: ubuntu-latest
    steps:
        # To use this repository's private action, you must check out the repository
        - name: Checkout
          uses: actions/checkout@v2

        - name: deploy file to server
          uses: wlixcc/SFTP-Deploy-Action@v1.2.4
          with:
              username: 'mckledumy'
              server: '128.199.233.104'
              # ssh_private_key: ${{ secrets.SSH_PRIVATE_KEY }}
              password: ${{ secrets.MCKL_FTP_PASSWORD }}
              local_path: './dist/*'
              remote_path: '/home/mckledumy/public_html'
              sftpArgs: '-o ConnectTimeout=5'

#           server: 128.199.233.104
#           username: mckledumy
#           password: ${{ secrets.MCKL_FTP_PASSWORD }}
#           server-dir: /home/mckledumy/public_html/
#           local-dir: dist/


      # - name: Download artifact
      #   uses: actions/download-artifact@main
      #   with:
      #     name: dist
      #     path: dist
      # - name: Sync files to production server
      #   uses: goblinfactory/konfig@v14
      #   with:
      #     command: |
      #       sftp -i ${{ secrets.MCKL_FTP_PASSWORD }} -P 22 mckledumy@128.199.233.104 <<EOF
      #       put -r dist/* /home/mckledumy/public_html/
      #       bye
      #       EOF


# name: Deploy to production via FTP
# on:
#   push:
#     branches:
#       - production
#   workflow_dispatch:

# permissions:
#   contents: read
#   pages: write
#   id-token: write

# jobs:
#   build:
#     name: build
#     runs-on: ubuntu-latest
#     env:
#       NODE_ENV: production
#     steps:
#       - name: Checkout repository
#         uses: actions/checkout@main
#       - name: Install dependencies
#         run: npm ci
#       - name: Build dependencies
#         run: npm run build
#       - name: Archive production artifact
#         uses: actions/upload-artifact@main
#         with:
#           name: dist
#           path: dist
#   deploy_ftp:
#     name: deploy_ftp
#     needs: build
#     runs-on: ubuntu-latest
#     steps:
#       - name: Download artifact
#         uses: actions/download-artifact@main
#         with:
#           name: dist
#           path: dist
#       - name: Sync files to production server
#         uses: SamKirkland/FTP-Deploy-Action@4.3.3
#         with:
#           server: 128.199.233.104
#           username: mckledumy
#           password: ${{ secrets.MCKL_FTP_PASSWORD }}
#           server-dir: /home/mckledumy/public_html/
#           local-dir: dist/
